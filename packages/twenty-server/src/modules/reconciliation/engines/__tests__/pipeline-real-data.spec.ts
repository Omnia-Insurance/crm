/**
 * Legacy integration test — covered the deprecated parseAmbetterBob /
 * computeFieldDiffs pipeline. Deleted along with those code paths in the
 * 2026-04-29 deprecated-cleanup pass. The unit specs in
 * `engines/__tests__/{matching,status,diff}.spec.ts` provide equivalent
 * coverage for the active pipeline.
 *
 * Safe to `git rm` this file.
 */
describe.skip('pipeline (legacy — removed)', () => {
  it.skip('placeholder', () => undefined);
});
